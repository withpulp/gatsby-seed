const composition = require('./_composition');
const content = require('./_content');
const palette = require('./_palette');
const structure = require('./_structure');
const types = require('./_types');

// Configure constants module exports.
module.exports = {
  composition,
  content,
  palette,
  structure,
  types,
};
