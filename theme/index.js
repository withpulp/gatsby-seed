const { composition, content, palette, structure, types } = require('./constants');

// Configure theme module exports.
module.exports = Object.assign(
    composition,
    content,
    palette,
    structure,
    types,
);
