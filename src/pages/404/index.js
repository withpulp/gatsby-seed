import React from 'react';

import Layout from 'containers/layout';

// TODO:
// - create 404 page using available components.

const NotFound = () => (
    <Layout>
        <p>Not found.</p>
    </Layout>
);

export default NotFound;
